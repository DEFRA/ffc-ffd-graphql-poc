{{- include "ffc-helm-library.container-config-map" (list . "ffc-ffd-graphql-poc.config-map") -}}
{{- define "ffc-ffd-graphql-poc.config-map" -}}
data:
  NODE_ENV: {{ quote .Values.environment }}
  PORT: {{ quote .Values.container.port }}
  APPINSIGHTS_CLOUDROLE: {{ quote .Values.name }}
{{- end -}}
